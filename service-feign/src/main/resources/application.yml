eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/ #http://eureka.didispace.com/eureka/
server:
  port: 8765
spring:
  application:
    name: service-feign

#feign默认集成hystrix 此为开启配置
feign:
  hystrix:
    enabled: true